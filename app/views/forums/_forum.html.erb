<tr>
  <td><%= link_to h(forum.title), forum_path( forum ) %></td>
  <td><%=h forum.description %></td>
  <td><%= link_to 'Редактировать', edit_forum_path( forum ) %></td>
  <td><%= button_to 'Удалить', forum_path( forum ), :method => :delete %></td>
</tr>
