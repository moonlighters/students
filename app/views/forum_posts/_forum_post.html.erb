<tr>
  <td>
    <a name="post<%= forum_post.id %>"/>
    <b><%= link_to_user forum_post.user %></b>
    <br/><%= format_time forum_post.created_at %></td>
  <td>
    <%= add_br_to_text( h forum_post.body ) %>
    <% unless forum_post.last_editor.nil? %>
    <br/><br/>
    <i><small>Последний раз редактировалось: <%= link_to_user forum_post.last_editor %>, <%= format_time forum_post.updated_at %></small></i>
    <% end %>
  </td>
  <td align="center"><%= link_to 'Править', edit_forum_post_path( forum_post ) %></td>
</tr>
